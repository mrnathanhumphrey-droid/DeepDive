# Secrets & credentials
.env
.env.local
credentials/token.json
credentials/oauth_client.json
credentials/*.pem
credentials/*.key

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.venv/
venv/
env/
.pytest_cache/

# Local data (server-side state, not shipped)
.chromadb/
.chromadb_test/
results/
logs/

# Log files
*.log

# IDE / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db

# Electron shell build artifacts
electron/node_modules/
electron/dist/
electron/out/

# Fly CLI local state
.fly/

# Scratch git worktree for offline approval workflow (auto-created by
# scripts/approve_run.py). Always cleaned up post-run, but guard against
# accidental commits if a prior session crashed.
.approvals_worktree/
